# coding:utf-8
class Ball:
    def _init_(self, x, y, dx, dy, color):
        self.x = x
        self.y = y
        self.dx = dx
        self.dy = dy
        self.color = color
    def test(self)
        print(self.x)
        print(self.y)